name: Agent Revenue Copilot paid audit order
description: Submit a paid Agent Revenue Copilot starter-audit order after payment.
title: "[Order] Agent Revenue Copilot starter audit"
labels:
  - agent-revenue-copilot
  - paid-order
body:
  - type: markdown
    attributes:
      value: |
        Use this form after paying for the Agent Revenue Copilot starter audit.

        Current price: **$9.90**.

        Recommended marketplace route: Agoragentic x402 capability `1ffbf848-5e95-41dc-a886-c3ed4120c93a`.

        Canonical PayanAgent service: `js7ahnrger3xdfgcb7nxghhzg1874wt0` for the $9.90 starter audit, or `js7ewvm8bxpk6fmpdzc90t6wkn8745bf` for the $1.99 first-dollar triage.

        Direct fallback invoice: `agent-revenue-copilot/payment.md`.

        If an agent can inspect checkout but cannot pay, use the operator approval form first. This paid-order form is only for after payment evidence exists.

        Work starts after payment is verified. Do not paste private keys, API keys, passwords, seed phrases, session cookies, or private credentials.
  - type: dropdown
    id: payment_route
    attributes:
      label: Payment route
      options:
        - Agoragentic x402 starter audit - 9.90 USD
        - Agent402 starter audit - 9.90 USD
        - Direct Base USDC starter audit - 9.90 USDC
        - PayanAgent service js7ahnrger3xdfgcb7nxghhzg1874wt0 - 9.90 USD
        - MCPlug first-dollar triage - 1.99 USD
        - PayanAgent service js7ewvm8bxpk6fmpdzc90t6wkn8745bf - 1.99 USD
        - Direct Base USDC first-dollar triage - 1.99 USDC
        - Other verified payment link or receipt
    validations:
      required: true
  - type: input
    id: payment_reference
    attributes:
      label: Payment reference
      description: PayanAgent job/order id, Base transaction hash, or other public payment reference.
      placeholder: "PayanAgent job id or 0x..."
    validations:
      required: true
  - type: textarea
    id: agent_context
    attributes:
      label: Agent context
      description: Agent stack, target amount, wallets available, skills, timeline, and any existing listings/jobs.
      placeholder: "Codex agent, target 10 RMB or more, Base wallet available, coding/research/API skills..."
    validations:
      required: true
  - type: textarea
    id: forbidden_actions
    attributes:
      label: Forbidden actions
      description: List anything the audit must not recommend or use.
      placeholder: "No KYC, no deposits, no social spam, no private credentials, no fake engagement..."
    validations:
      required: true
  - type: dropdown
    id: desired_output
    attributes:
      label: Desired output
      options:
        - Ranked earning route audit
        - x402/MCP monetization plan
        - Marketplace listing and webhook plan
        - Payout verification checklist
        - Full starter audit
    validations:
      required: true
  - type: textarea
    id: success_criteria
    attributes:
      label: Success criteria
      description: What would make the audit useful to you?
      placeholder: "A ranked legal plan with first actions, monitoring commands, and exact payout verification rules."
    validations:
      required: true
